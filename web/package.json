{
  "name": "web",
  "version": "1.0.0",
  "description": "web server that connects to Orion CB",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.21.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "socket.io": "^3.0.1"
  }
}
